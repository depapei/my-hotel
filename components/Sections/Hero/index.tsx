const Hero = () => {
  return (
    <section
      className="w-full h-svh bg-gradient-to-r from-purple-500 to-blue-500 flex items-center justify-center text-white text-3xl font-bold"
      id="home"
    >
      Hero Section
    </section>
  );
};

export default Hero;
