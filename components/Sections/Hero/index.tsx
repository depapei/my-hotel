const Hero = () => {
  return (
    <section
      className="w-full h-svh bg-blue-500 flex items-center justify-center text-white text-3xl font-bold"
      id="home"
    >
      Hero Section
    </section>
  );
};

export default Hero;
